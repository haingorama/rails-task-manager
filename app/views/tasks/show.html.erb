<h1>My tasks</h1>

<%= link_to("Edit", edit_path) %>

<%= @task.title %> <br>
<%= @task.details %>

<%= link_to("Retour", tasks_path) %>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
